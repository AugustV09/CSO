Para acceder a los parametros enviados al momento de la invocación del script, es necesario utilizar el simbolo $, seguido del numero 1,2,3,... Por ejemplo, debe utilizarse $1, $2, $3, ...
Dentro de un script, $# indica la cantidad de argumentos recibidos (parametros enviados); $* contiene una lista de todos los argumentos recibidos; $? contiene en todo momento el valor de retorno del ultimo comando ejecutado; $HOME indica el path del directorio home del usuario en sesion.